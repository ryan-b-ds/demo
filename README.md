Our group tested the github workflow process together on a default maven JavaFX project. 
We had some struggles with using git as a group, especially when it came to how to handle step 3, where we needed to record our part of the workflow. 
Although Ryan made 4 commits attempting to revert to initial commit, we were unable to do so due to his VSCode causing issues at an unfortunate time.
In the end, we learned to instead demonstrate our workflow without reverting to initial commit. 
Our video features each member adding a comment to the JavaFX project, as well as Grey successfully running the app and clicking the buttons in the window.
